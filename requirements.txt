# Core dependencies
numpy>=1.21.0,<2.0.0
pandas>=1.3.0,<2.0.0
scipy>=1.7.0,<2.0.0

# Deep Learning
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0
torchaudio>=2.0.0,<3.0.0

# Computer Vision
opencv-python>=4.5.0,<5.0.0
dlib>=19.22.0,<20.0.0
face-recognition>=1.3.0,<2.0.0

# Audio Processing
librosa>=0.9.0,<1.0.0
soundfile>=0.10.3,<1.0.0
pydub>=0.25.1,<1.0.0
scipy>=1.7.0,<2.0.0  # Required for audio processing

# Video Processing
moviepy>=1.0.3,<2.0.0
ffmpeg-python>=0.2.0,<1.0.0
# PyAV is installed separately using: pip install av --no-binary av --prefer-binary

# Web Interface
streamlit>=1.22.0,<2.0.0
gradio>=3.32.0,<4.0.0

# Utilities
tqdm>=4.62.0,<5.0.0
scikit-learn>=1.0.0,<2.0.0
matplotlib>=3.4.0,<4.0.0
seaborn>=0.11.0,<1.0.0
pillow>=9.0.0,<10.0.0  # Required for image processing
requests>=2.26.0,<3.0.0  # For downloading models/weights

# Development
pytest>=7.0.0,<8.0.0
black>=22.0.0,<23.0.0
flake8>=4.0.0,<5.0.0
mypy>=0.950,<1.0.0  # For type checking
isort>=5.10.0,<6.0.0  # For import sorting

# Optional but recommended
tensorboard>=2.8.0,<3.0.0  # For training visualization
wandb>=0.12.0,<1.0.0  # For experiment tracking

# Installation Notes:
# 1. Install PyTorch first:
#    pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 2. Install dlib using pre-built wheel (adjust Python version as needed):
#    pip install https://github.com/jloh02/dlib/releases/download/v19.22/dlib-19.22.99-cp310-cp310-win_amd64.whl
#
# 3. Install PyAV separately:
#    pip install av --no-binary av --prefer-binary
#
# 4. Then install remaining requirements:
#    pip install -r requirements.txt 